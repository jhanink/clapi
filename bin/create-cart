. ./include/script-include

if [ $HAS_PARAMS == 1 ]
then
  node $PROG --name=$SCRIPT_NAME --customerId=$1 "$@" > $BASE_DIR/output/$SCRIPT_NAME.json
  cat $BASE_DIR/output/$SCRIPT_NAME.json >
fi

node $PRINT_OUTPUT --file=$BASE_DIR/output/$SCRIPT_NAME.json "$@"