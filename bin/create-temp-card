if [ `basename "$PWD"` == "bin" ]; then PROG='../index'; else PROG='index'; fi

node $PROG --name=get-customer --customerId=$1 --RAW > /tmp/customer_$1.json

node $PROG --name=create-temp-card --file=/tmp/customer_$1.json
