. ./include/script-include

if [ $HAS_PARAMS == 1 ]
then
  node $INDEX --name=$SCRIPT_NAME --customerId=$1 --RAW > $BASE_DIR/output/$SCRIPT_NAME.json
fi

node $PRINT_OUTPUT --file=$BASE_DIR/output/$SCRIPT_NAME.json "$@"
