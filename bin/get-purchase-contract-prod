. ./include/script-include

if [ ! -z "$1" ]
then
  node $PROG --name=get-purchase-contract --pcId=$1 --PROD "$@" > $BASE_DIR/output/get-purchase-contract-prod.json
fi

cat $BASE_DIR/output/get-purchase_contract_prod.json